# Site template

Base Next.js utilisée par l'agence pour chaque nouveau site. Le design system vit dans `app/globals.css` (variables CSS) : l'agent Dev y injecte les valeurs de la maquette. Les composants de `components/` sont les briques que l'agent assemble. Voir `CLAUDE.md`.
